import { keyframes } from '@emotion/react';

export const typing = keyframes`
  from { width: 0; }
  to { width: 58%; }
`;
export const typingSecond = keyframes`
  from { width: 0; }
  to { width: 90%; }
`;
export const typingThird = keyframes`
  from { width: 0; }
  to { width: 110%; }
`;
export const typingFour = keyframes`
  from { width: 0; }
  to { width: 85%; }
`;
export const typingFive = keyframes`
  from { width: 0; }
  to { width: 130%; }
`;
export const typingSix = keyframes`
  from { width: 0; }
  to { width: 95%; }
`;

export const colorChange = keyframes`
  0% { color: #333; }
  20% { color: #ce5ff8; }
  40% { color: #ff6f61; }
  60% { color: #ce5ff8; }
  80% { color: #4caf50; }
  100% { color: #333; }
`;

export const blinkEffect = keyframes`
  0%, 100% { border-color: transparent; }
  50% { border-color: white;border-right: 3px solid white }
`;

export const opasityIcon = keyframes`
from {opacity: 0}
to {opacity: 1}
`;
